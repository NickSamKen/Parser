public class SPCParser {
    public String getNextToken() //returns the next token that is not a comment
    {

        return "return the next string not a comment";
    }

    public Boolean identifierExists(String identifier)// returns true if identify has already been declared.
    {
        return true;
    }

    public void /*might need a return type*/ begin(keyWords token){
        start(token);
    }

    private void start(keyWords token)
    {

    }
}
